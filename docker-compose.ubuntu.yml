services:
  samba-ad:
    build:
      context: .
      dockerfile: Dockerfile.ubuntu
    container_name: samba4-ad
    hostname: dc1
    network_mode: "host"
    env_file:
      - .env
    volumes:
      - ubuntu_samba_etc:/etc/samba
      - ubuntu_samba_lib:/var/lib/samba
      - ubuntu_samba_logs:/var/log/samba
    tty: true
    stdin_open: true

volumes:
  samba_etc:
  samba_lib:
  samba_logs:
